version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: hjh538-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: hjh538
      MYSQL_USER: hjh538
      MYSQL_PASSWORD: password
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-ppassword"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hjh538-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # server:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile.server
  #   container_name: hjh538-server
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=production
  #     - DATABASE_URL=mysql://hjh538:password@mysql:3306/hjh538
  #     - REDIS_HOST=redis
  #     - REDIS_PORT=6379
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   restart: unless-stopped

  # web:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile.web
  #   container_name: hjh538-web
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - server
  #   restart: unless-stopped

volumes:
  mysql_data:
  redis_data:
